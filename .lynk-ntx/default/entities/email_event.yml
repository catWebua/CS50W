name: email_event

description: ''

key_table: networx_prod.communications.email_events

aliases:
- event

related_assets:
  networx_prod.communications.cio_email_events:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: event_id
        destination: event_id
        operator: equal
  networx_prod.communications.email_events:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: event_id
        destination: event_id
        operator: equal

features: []
