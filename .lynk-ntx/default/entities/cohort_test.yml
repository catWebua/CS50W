name: cohort_test

description: ''

key_table: networx_src.networx_global.ng_client_cohort_tests

aliases: []

related_assets:
  networx_src.networx_global.ng_client_cohort_tests:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: cohort_test_id
        destination: cohort_test_id
        operator: equal

features: []
