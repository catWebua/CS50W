name: state

description: ''

key_table: networx_src.networx_global.ng_state

aliases: []

related_assets:

  networx_src.networx_global.ng_state:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: state_id
        destination: state_id
        operator: equal
  

features: []
