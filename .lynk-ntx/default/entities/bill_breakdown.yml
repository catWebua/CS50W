name: bill_breakdown

description: ''

key_table: networx_prod.finance.bill_breakdown_dim

aliases: []

related_assets:
  networx_prod.core.product_dim:
    relationship: many_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: product_id
        destination: product_id
        operator: equal
  networx_prod.finance.bill_breakdown_dim:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: bill_breakdown_id
        destination: bill_breakdown_id
        operator: equal

features: []
