name: client_user

description: ''

key_table: networx_prod.core.client_user_dim

aliases: []

related_assets:
  networx_src.networx_global.ng_user_excluded_classifications:
    relationship: one_to_many
    joins:
    - type: fields
      default: true
      fields:
      - source: client_to_user_id
        destination: client_to_user_id
        operator: equal
  networx_prod.core.client_user_dim:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: client_to_user_id
        destination: client_to_user_id
        operator: equal

features: []
