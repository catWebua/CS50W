name: payment_method

description: payment method (credit card details)

key_table: networx_src.networx_global.ng_creditcard_details

aliases:
- method

related_assets:
  networx_src.networx_global.ng_payment_methods_vault_log:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: method_id
        destination: method_id
        operator: equal
  networx_src.networx_global.ng_creditcard_details:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: method_id
        destination: method_id
        operator: equal

features: []
