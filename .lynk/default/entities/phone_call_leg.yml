name: phone_call_leg

description: ''

key_table: networx_prod.communications.phone_call_leg_dim

aliases:
- call_leg

related_assets:
  networx_prod.communications.phone_call_leg_dim:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: call_leg_id
        destination: call_leg_id
        operator: equal

features: []
