name: ad

description: ''

key_table: networx_prod.marketing.google_ads_dim

aliases: []

related_assets:
  networx_src.google_ads.gads_ads_metadata:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: ad_id
        destination: ad_id
        operator: equal
  networx_prod.marketing.google_ads_dim:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: ad_id
        destination: ad_id
        operator: equal

features: []
