name: network

description: ''

key_table: networx_src.networx_global.ng_networks

aliases: []

related_assets:
  networx_src.networx_global.ng_networks:
    relationship: one_to_one
    joins:
    - type: fields
      default: true
      fields:
      - source: network_id
        destination: network_id
        operator: equal

features: []
